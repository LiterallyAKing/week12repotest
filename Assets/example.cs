using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class example : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
		//TODO: make this script do things.
		int a = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
